{
  "name": "fronted-next",
  "type": "module",
  "version": "3.0",
  "private": true,
  "description": "哈基密语加密器Next版前端工程",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc -b",
    "generate-vue-routes": "npx tsx ../scripts/generate-routes.ts"
  },
  "dependencies": {
    "pinia": "^3.0.3",
    "vue": "^3.5.17",
    "vue-router": "4"
  }
}
